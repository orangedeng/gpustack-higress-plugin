# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**GPUStack Higress Plugins** - A Python package that distributes Higress Proxy-Wasm plugins for GPUStack, with a FastAPI-based HTTP server for serving them.

- **Go plugins** in `extensions/` - Compiled to WebAssembly (`.wasm`) for Higress
- **Python package** `gpustack_higress_plugins/` - FastAPI server that serves the built `.wasm` files
- **Remote plugins** - Support fetching plugins from OCI registries (oras)

## Common Commands

### Setup

```bash
make venv         # Create .venv with python3 -m venv (NOT uv venv)
make install      # Install runtime deps with uv sync
make dev          # Install dev deps + pre-commit install (hooks run on git commit)
```

### Build

```bash
make build        # Build local Go plugins + fetch remote plugins (requires oras) + generate manifest + build wheel
make build-docker # Build wheel via Docker (for environments without Go toolchain or oras)
make clean        # Remove build artifacts in dist/ (uv.lock is preserved)
make clean-venv   # Remove .venv
make verify-whl   # Verify wheel contents against expected plugins (runs verify_whl.py)
```

`make build` gracefully skips remote plugins if `oras` is not installed (prints a warning).

### Go Plugins (run from repo root or via -C)

```bash
make -C extensions build PLUGIN_NAME=<name>   # Build single plugin
make -C extensions build-all                   # Build all local plugins only
make -C extensions build-all-with-remote       # Build local + fetch remote plugins (warn if oras missing)
make -C extensions watch PLUGIN_NAME=<name>    # Watch + rebuild (requires: brew install fswatch)
make -C extensions list                        # List local plugins with versions
make -C extensions list-remote                 # List remote plugins from remote_plugins.yaml
make -C extensions fetch-remote PLUGIN='oci://registry/name:tag|local-name|version'  # Fetch single remote plugin
make -C extensions fetch-all-remote               # Fetch all remote plugins from remote_plugins.yaml
```

### Testing

```bash
make test                                          # Run all Go plugin tests
make -C extensions test PLUGIN_NAME=<name>         # Test single plugin (go test -v ./...)
make -C extensions test-all                        # Test all plugins (stops on first failure)
```

### Lint and Format

```bash
make lint         # ruff check (rules: E, F, I, N, W; ignores E501) on gpustack_higress_plugins/ and scripts/
make format       # ruff check --fix + ruff format
make check-dirty  # Verify no uncommitted changes (used in CI)
```

### Docker

```bash
make image        # Build Docker image
GOPROXY=https://goproxy.cn,direct make image  # Build with custom Go proxy
make push         # Push to registry
```

## Architecture

### Python Package (`gpustack_higress_plugins/`)

- `main.py` - CLI entry point (`gpustack-plugins`), FastAPI app factory `create_app()`:
  - Mounts `server.py` router
  - Registers `GET /` → `{"status": "ok", "version": ...}` (health check endpoint)
  - Suppresses uvicorn access logs for `GET /` (health check noise filter)
- `server.py` - APIRouter at prefix `/wasm-plugins` with endpoints:
  - `GET /wasm-plugins/{name}/{version}/plugin.wasm`
  - `GET /wasm-plugins/{name}/{version}/metadata.txt`
- `plugins/` - Built `.wasm` + `metadata.txt` files (generated, not in git)
- `manifest.json` - Generated plugin index (not in git)

**CLI flags**: `gpustack-plugins start --port 8080 --host 0.0.0.0 --log-level [critical|error|warning|info|debug]`

**Embedding**: `from gpustack_higress_plugins import router, create_app` (embed in another FastAPI app)

**Wheel packaging**: uses `[tool.hatch.build.targets.wheel.force-include]` to include gitignored `plugins/` and `manifest.json`. Built with `uv build --wheel` (not `uv build`) to avoid the sdist→wheel path which can't find gitignored files.

### Go Plugins (`extensions/`)

Each plugin is an independent Go module with `main.go`, `go.mod`, `VERSION`. All modules are listed in `go.work`.

- **Build target**: `GOOS=wasip1 GOARCH=wasm -buildmode=c-shared`
- **Go version**: 1.24.4
- **Key deps**: `proxy-wasm-go-sdk`, `higress-group/wasm-go`, `tidwall/gjson`, `tidwall/sjson`

The `metadata.txt` (Higress standard) is generated by `scripts/generate_metadata.py` with name, size, timestamps, and MD5.

**`PYTHON=true` trick**: `extensions/Makefile` defines `PYTHON ?= ...` (conditional assignment). Passing `PYTHON=true` on the command line overrides it so metadata calls become `true /path/to/generate_metadata.py ...` — the Unix `true` command ignores all arguments and exits 0. Used in the Docker go-builder stage so Python is not required there.

**Metadata generation context**:

- Local dev (`make build`): generated during `extensions/Makefile build-all` by calling `generate_metadata.py` via the local Python
- Docker build: go-builder runs `make build-all PYTHON=true` (metadata skipped); whl-builder then generates metadata with a `find ... | while read` loop after copying plugins from go-builder

### Scripts (`scripts/`)

- `generate_metadata.py` - Creates `metadata.txt` per plugin
- `generate_manifest.py` - Scans `plugins/` + `remote_plugins.yaml` → `manifest.json`; version sorting via `_version_sort_key()`; reads package version via `tomllib` (Python 3.11+) or `tomli` (3.10)
- `fetch_remote_plugins.py` - Pulls OCI plugins via oras; supports two modes:
  - Config-file mode: `--config remote_plugins.yaml` (fetches all configured plugins)
  - Single-plugin mode: `--source oci://... --name <name> --version <ver>`

### Version Management

- **Package version**: `pyproject.toml` holds placeholder `0.0.0`; CI (`release.yaml`) overwrites it from the git tag before building
- **Plugin version**: `extensions/<name>/VERSION` file (individual version string, e.g., `1.0.0`)
- Runtime version read via `importlib.metadata.version()` with fallback to pyproject.toml parsing (development without install)
- **Python requirement**: `>=3.10`; `tomli>=2.0` installed as dev dep on Python 3.10 (not needed on 3.11+)

## Plugin Details

### gpustack-token-usage

Collects token usage stats from AI API responses. Key non-obvious behaviors:

- Only processes requests with `Content-Type: application/json` or `multipart/form-data`
- For multipart requests, model name is extracted from the form field named `"model"`
- Streaming detection uses `stream` field from **request body** (not response `Content-Type`)
- Auto-injects `stream_options.include_usage: true` into request body when needed
- If request has `stream: true` but response `Content-Type: application/json`, treats as non-streaming
- **Streaming vs non-streaming response handling diverges sharply (don't collapse them):**
  - *Streaming (SSE)*: the plugin **rewrites** chunks (injects TTFT/TPOT/TPS into usage, strips the include_usage chunk). To get plaintext it can edit, `processRequestBody` **strips `accept-encoding` from the upstream request** when `IsStreamingResponse && ProcessBody`. Usage parsed incrementally per chunk in `processTokenUsage`.
    - **SSE parsing is event-aware (don't revert to a naive `TrimPrefix("data: ")`).** Each `\n\n`-delimited block is split by `splitSSEEvent` into the non-data prefix lines (`event:` / `id:` / comments) and the concatenated `data:` payload. The OpenAI **Responses API** emits `event: <type>\ndata: {...}` blocks — a bare `TrimPrefix` left the `event:` line in front of the JSON, so `json.Valid` failed and the block was mis-buffered as an "incomplete chunk", corrupting the stream and losing usage entirely. `reassembleSSEEvent` re-emits the prefix so Responses clients that dispatch on the event type still work.
    - **Usage lives at two paths**, resolved by `usageJSONPath`: top-level `usage` (Chat Completions, vLLM, Anthropic `message_delta`) or `response.usage` (Responses API). Extras (`process_data_with_token`) are injected at whichever path matched. The Responses `response.completed` event embeds the whole response object so it is large and frequently split across Envoy delivery chunks — `mergeSSEEventState` (the pure core of `mergeLargeUsageChunks`, unit-tested in [sse_event_test.go](extensions/gpustack-token-usage/sse_event_test.go)) buffers the **whole raw block, event prefix included**, until the data payload parses as JSON.
    - **`mergeSSEEventState` forwards any non-`data:` block verbatim** (SSE control lines, keep-alive comments, and non-SSE JSON bodies such as a rate-limit rejection). This is a behavior change from the old code, which ran `json.Valid` on the whole block and **ate** anything non-JSON into `IncompleteChunkData`. Net effect: a single-chunk rejection body (JSON *or* plain text) now passes through to the client instead of being silently dropped — see the rate-limit "Rejection delivery hazards" note below, which the old eating behavior motivated.
    - `isOutputDeltaChunk` also counts Responses-API deltas (any event `type` ending in `.delta` carrying a non-empty string `delta`).
  - *Non-streaming (embeddings, `stream:false`, or a `stream:true` request answered with one JSON body)*: **sniff-only — body forwarded byte-for-byte**, so `accept-encoding` is left intact and the client keeps gzip+chunked. `handleNonStreamingResponseBody` buffers raw chunks into `NonStreamBodyBufferKey`, then at `endOfStream` calls `decodeResponseBody` (per `Content-Encoding` captured in `ResponseContentEncodingKey`) and parses usage once. **No `tokens_per_second` injection on this path** (TTFT/TPOT are meaningless for a one-shot response; the field is client-only and not in the metrics report, so dropping it was confirmed safe). The old per-chunk parse silently lost usage for gzipped/large embeddings responses — the usage tail landed in a compressed, fragmented chunk gjson couldn't read.
  - `decodeResponseBody` supports only `identity`/`gzip`; other encodings (e.g. `br`) log a warn and the request is reported without tokens (fail-open). It's a pure function (no host calls) so it's unit-tested in [decode_response_test.go](extensions/gpustack-token-usage/decode_response_test.go).
  - **Both the accumulated buffer and the gzip-decoded output are capped by `maxResponseBodyBytes` (config field, default 10 MiB / `defaultMaxResponseBodyBytes`).** The buffer cap guards against an OOM from an enormous response; the decode cap (an `io.LimitReader(r, max+1)` with an explicit overflow check) guards against a zip bomb — a tiny gzip payload inflating without limit. `BufferExceededKey` latches once the buffer overflows so later chunks short-circuit. Over-cap bodies are dropped from usage tracking (no tokens recorded) but never blocked. The identity path isn't separately decode-capped because the raw buffer cap already bounds it; only gzip needs the second cap because compressed bytes inflate.
- `enableOnPathSuffix` values must start with `/`
- Real IP extracted via `net.SplitHostPort` to strip port from `source.address`
- Cluster name property (`cluster_name`) is parsed to extract model/provider IDs for metrics; requests whose cluster name doesn't match the expected format are skipped
- **Two ids are reported so an operator can locate an audit row from what a caller has in hand.** `request_id` is Envoy's `x-request-id`, captured in `prepareMetrics`; it exists for every tracked request regardless of endpoint or outcome and matches the access log, which is what makes it the lookup key. Envoy does *not* return it downstream (`always_set_request_id_in_response` defaults to false), so `echoRequestID` writes it onto the response under `requestIDResponseHeader` (default `X-GPUStack-Request-Id`, `""` disables) — a distinct name because some upstreams set an `x-request-id` of their own on the response. That echo runs in the response-header phase, which also fires for a **local reply**, so a rate-limit 429 carries the header even though `reportMetrics` deliberately writes no row for it. `upstream_response_id` is the model's own id, probed by `upstreamResponseID` (pure, tested in [response_id_test.go](extensions/gpustack-token-usage/response_id_test.go)) at `id` → `response.id` → `message.id` → `request_id` and short-circuited once captured. **This plugin does not see raw provider bodies** — Higress sorts wasm plugins descending by priority for the request chain (`push_context.go`, `iPriority > jPriority`) and Envoy reverses that for encoder filters, so ai-proxy (priority 100) runs *ahead* of token-usage (400) on the response path and has already mapped qwen's `request_id` onto `id`. The `request_id` probe is a hedge for ai-proxy's `protocol: original` passthrough (supported, not emitted by gpustack today), ordered last so a converted body's own `id` wins. The probe is otherwise **deliberately shape-agnostic — do not gate it on the endpoint**: vLLM stamps an `id` on embeddings responses (`embd-…`, see `embeddingsBody()` in [decode_response_test.go](extensions/gpustack-token-usage/decode_response_test.go)) where OpenAI's shape has none, and that is the only id an embeddings call has to be located by. The string-type check is what bounds it instead, since gjson's `String()` renders a numeric `id`/`index` as a plausible fake.
- **`ttft_ms` is on the report but measures the first response *chunk*, not the first token** — usually a role-only delta, or `event: response.created` on the Responses API. Kept that way on purpose: it is the same number the client already sees as `usage.time_to_first_token_ms`, and two figures under one name that disagree is worse than one that is slightly optimistic. Streaming only. **No `duration_ms` field**: `started_at`/`completed_at` are both on the wire and both stored, so a duration would only be a third number that can disagree with them.

Config fields: `realIPToHeader`, `enableOnPathSuffix`, `organizationIDHeader`, `requestIDResponseHeader`, `maxResponseBodyBytes`

### gpustack-set-header-pre-route

Injects route/cluster name into request headers **before** routing (pre-route phase).

Config fields: `routeNameHeader`, `clusterNameHeader`, `enableOnPathSuffix`, `enableOnPathPrefix`

### gpustack-rate-limit

Redis-backed multi-dimensional sliding-window rate limiter. User-facing config / YAML examples live in [extensions/gpustack-rate-limit/README.md](extensions/gpustack-rate-limit/README.md); the notes below only capture design decisions and hazards you'd otherwise need to reconstruct from the code.

**Envoy runtime properties**: declared `phase: UNSPECIFIED_PHASE`, `priority: 600` — intentionally aligned with Higress `ai-token-ratelimit`. The plugin must run **after** whoever publishes the `ai_log` filter state in the response-phase filter order; `ai-statistics` (`CUSTOM`/`200`) and `gpustack-token-usage` both qualify. Changing this priority below `200` will silently break TokenLimits.

**Config model** ([config.go](extensions/gpustack-rate-limit/config.go)):

- `PluginConfig.LimitCombinations[]`. Each combo = a list of `MatchRule` + any of: two `RateQuota` blocks (`QueryLimits` rolling-window request-count, `TokenLimits` rolling-window tokens) and one `QuotaSpec` block (`TokenQuota`, calendar-aligned token quota). `RateQuota` windows resolve in the order `PerSecond → PerMinute → PerHour → PerDay → PerCustom`; all unset returns `(0, 0)` and the limit is skipped. **No** `QueryQuota` slot exists by design — rolling `QueryLimits` already covers the "N requests per day" anti-abuse case; AI billing is token-based so calendar alignment is only meaningful for tokens.
- `PluginConfig.Timezone` is deployment-wide, top-level (default `UTC`). It anchors every `TokenQuota` boundary in this config; per-spec timezone fields are intentionally not exposed because a single deployment almost always has one timezone, and putting it on each spec invites mismatched-boundary bugs. `Validate` loads it once into `c.location` and pushes the `*time.Location` into every `TokenQuota.Compile(loc)`. The deployment timezone is **not** encoded into the Redis key — changing it reinterprets every existing key under the new boundary, which is the user-intended behaviour.
- `QuotaSpec` (calendar-aligned token quota): exactly one of `EachDay` / `EachMonth` / `EachYear` is set. `Compile(loc)` resolves the period kind, validates `limit > 0`, captures the location supplied by `PluginConfig.Validate`. `GetWindowAndQuota(now)` returns `(now - periodStart, limit)`; window is clamped to ≥ 1s so the lua `ZRANGEBYSCORE` range is never degenerate at the boundary. `KeyPart()` returns a stable label (`each_month` / `each_year`) — **not** a window-second string and **not** the timezone — so the Redis key is timezone-independent and does not rotate within a period.
- `MatchRule.Value` is a string alias whose match strategy is inferred at `Compile`: literal `"*"` → wildcard; `source: ip` → CIDR/IP; `"regexp_capture:<pattern>"` → RE2 with capture-group key extraction (pattern must contain ≥1 `(...)`); `"regexp:<pattern>"` → RE2 (whole extracted value used as key); otherwise exact. Compile caches regexp/CIDR inside the rule; `Match(headers) *string` returns the extracted raw value on a hit (or `m[1]` on a `regexp_capture:` hit), `nil` on a miss or an uncompiled rule.
- **`regexp_capture:` is the only way to bucket two different inputs together** when they share a common substring (e.g. `<ak>.gpustack-1` and `gpustack-1` both bucket as `gpustack-1`). The `(?:...)` non-capturing group syntax is essential for the prefix half. Without `regexp_capture`, `Match` returns the full extracted header value and the two shapes go into separate Redis buckets.
- `SourceConsumer` is hard-wired to read header `x-mse-consumer` (Higress convention, constant `ConsumerHeader`). `SourceIP` is a header-source special case: `Name` holds the header that carries the already-injected real client IP (e.g. `x-real-ip`).
- `PluginConfig.Validate()` both validates and pre-compiles every `MatchRule` — `parseGlobalConfig` / `parseOverrideRuleConfig` each call it exactly once at startup so there are no runtime compile errors.
- Global-level config may omit `LimitCombinations` (then Validate is skipped). This supports the pattern "global supplies Redis/defaults, per-route rules override via `ParseOverrideConfig`".
- **`redis` field detection uses `gjson.Result.IsObject()`**, not `Exists()`. `Exists()` returns `true` for an explicit `redis: null` literal (the gjson contract is `Type != Null || len(Raw) != 0`, and Raw is the string `"null"`), which would route into `initRedisBackend` and fail with "service_name must not be empty". When `parseGlobalConfig` returns an error, wasm-go's rule_matcher swallows it but leaves `m.globalConfig` as the zero value, so every matchRule's `parseOverrideRuleConfig` then inherits empty path filters and the plugin silently no-ops every request with `path "..." not enabled by path filters`. Don't revert this back to `Exists()`.
- **`limit_combinations` aggregation semantics**: `parseOverrideRuleConfig` appends rule-level combos onto global combos rather than replacing them. Every request matched by a rule runs the **union** in a single Redis Eval — global combos first (broader scope, shorter-circuit on common over-quota), then rule-level combos. Unique combo names are enforced across the merged list by `Validate`, so a rule cannot silently shadow a global combo. This is the load-bearing mechanism that lets deployment-wide buckets (e.g. cross-model per-consumer monthly quota) live in `defaultConfig` while each ingress only declares its model-specific sub-cap.
- **Aliasing safety in `parseOverrideRuleConfig`**: `*config = global` shares all slice headers (`LimitCombinations`, `EnableOnPathSuffix`, `EnableOnPathPrefix`) with global. `json.Unmarshal` of slice fields **resets-and-reuses** the underlying array, which would corrupt global through aliasing across matchRule overrides. The parser **must** detach all three slices to nil before `json.Unmarshal` and rebuild them via `append([]T(nil), global....)` afterwards. This is a real latent footgun — don't simplify the parser back into "just `*config = global` then unmarshal".

**Redis key layout** (one per quota kind):

```text
<rule_name>|<combo.Name>|<dim1>|<dim2>|...|<kind>:<period>
```

`<kind>` is `q` (query-count) or `t` (token-count). `<period>` is the window-second string (e.g. `60s`) for rolling `RateQuota`, or the stable calendar label (e.g. `each_month`, `each_year`) for `QuotaSpec`. Each dim fragment is `<source>:<name>=<value>` or `<source>=<value>` when `name` is empty / for consumer. ZSET member is `<request-uuid>|<count>` (count=`1` for query, real `total_tokens` for token). If rule-level overrides leave `rule_name` inherited from global and combo names collide, two routes share the same key — `rule_name` is the only cross-route isolator.

**Calendar quota mechanics**: `TokenQuota` reuses the existing lua script unchanged. The plugin computes `window = now - periodStart` at request phase and again at response phase (so a stream that crosses the boundary records its tokens into the period it ends in). The lua `ZREMRANGEBYSCORE -inf (now-window)` therefore prunes everything before the current period start; a clamped minimum window of 1s avoids deleting boundary-second writes. `EXPIRE key (window*2)` shrinks toward 2s right at the boundary, which conveniently lets stale period data evaporate within seconds without any external reset job.

**Lua script** ([multi_level_limit.lua](extensions/gpustack-rate-limit/multi_level_limit.lua)) — contract is per-combination 6-tuple ARGV `[window, quota, mode, member, count, ttl]` with `mode ∈ { check_and_add, check, add }`. Two-pass:

- Pass 1 — `ZREMRANGEBYSCORE` then, for every `check` or `check_and_add` combo, sum `count` across members via `ZRANGEBYSCORE` + string-match `|(%d+)$`. First over-quota combo short-circuits with `{'LIMITED', <key>}`. **No** combination is written in this case.
- Pass 2 — `ZADD` + `EXPIRE <ttl>` for every `add` or `check_and_add` combo.
- Reply is always a two-element Lua array: `{'SUCCESS', ''}` on pass, `{'LIMITED', <key>}` on block. The Go callback reads it with `response.Array()` — **do not** switch to `response.String()` / `response.Error()`; see "Rejection delivery hazards" below.

**TTL is decoupled from `window`** (this was a bug fix): the lua script no longer derives EXPIRE from `window * 2`. Calendar quotas pass a dynamic `window = now - period_start` which is near-zero at period start; multiplying that by 2 gave a 1-2 second TTL and the key would be evicted before the next request, prematurely resetting the quota. Go-side helpers (`rollingTTL` / `calendarTTL` in main.go) compute TTL explicitly: rolling = `window * 2` (preserved); calendar = `period_end - now + ttlGraceSeconds` (300s grace). Don't reintroduce `window * 2` in the lua script.

Request-phase `Eval` uses `check_and_add` (count=1) for `QueryLimits` and `check` (count=0) for both `TokenLimits` and `TokenQuota`. Response-phase (`onHttpStreamDone`) issues a second `Eval` with `mode=add` and `count=total_tokens` for every pending entry. `tokenPendingAdd` carries either a `FixedWindow` (RateQuota) or a `CalendarSpec *QuotaSpec` (QuotaSpec); `windowSeconds(now)` dispatches to recompute the calendar window fresh at response time. `total_tokens` is read from the Envoy filter state `wrapper.AILogKey` — the plugin **does not** parse SSE itself.

**Path filter**: `EnableOnPathSuffix` / `EnableOnPathPrefix` default to AI endpoints + `/model/proxy`. `isPathEnabled()` runs before anything else in the request-header phase; non-matching paths skip the full path — no `GetHttpRequestHeaders()`, no Redis round-trip. `matchPathFilters` is split out as a pure function so it's unit-testable without a proxy-wasm host.

**Unit tests** ([config_test.go](extensions/gpustack-rate-limit/config_test.go), [main_test.go](extensions/gpustack-rate-limit/main_test.go)): standard `go test` (no wasm host), cover every branch of match / compile / extract / KeyPart / Validate / collectChecks / matchPathFilters.

**VM rebuild**: `WithRebuildAfterRequests(1000)` + `WithRebuildMaxMemBytes(200 MiB)` — aligned with Higress AI plugin conventions, guards against long-running memory-leak accumulation.

**Metrics** ([metrics.go](extensions/gpustack-rate-limit/metrics.go)): three counters (`request_total` / `rejected_total` / `value_total`) emitted via `proxywasm.DefineCounterMetric`. Stat names follow the Higress AI plugin convention `route.<route>.upstream.<cluster>.model.<model>.consumer.<consumer>.metric.<metric>.<extras>` so the existing bootstrap stats_tags (defined in Higress's [`envoy_bootstrap.json`](https://github.com/alibaba/higress/blob/main/istio/istio/tools/packaging/common/envoy_bootstrap.json)) auto-extract `ai_route` and `ai_cluster` as proper Prometheus labels — `ai_model` is NOT extracted because Higress's regex requires an `(input_token|output_token)` terminal which our metric names don't have. The remaining slots (`model`, `consumer`, `rule`, `combo`, `kind`, `bucket`, `result`) live in the metric-name string; the README documents a `metric_relabel_configs` recipe to flatten them into Prometheus labels at scrape time. We deliberately do NOT ship an EnvoyFilter manifest — the limitation is shared by the entire Higress AI plugin ecosystem (only the four `ai_*` slots get auto-extracted) and an EnvoyFilter is only meaningful when paired with Prometheus + Grafana dashboards, which are a separate deliverable. `metricCounters` is a process-global `sync.Map` cache because counter handles are owned by the host (idempotent across configs and across VM rebuilds). `request_total` is intentionally only labelled by `route, rule, result` (plus the AI prefix slots) so `sum(request_total)` is the real request count; per-combo breakdown lives on `rejected_total` and `value_total`. `rejected_total` is incremented *after* `rejected()` returns to keep the path between callback entry and `SendHttpResponseWithDetail` minimal (same hazard as the redis async case in CLAUDE.md). The four AI-routing labels (`route`/`cluster`/`model`/`consumer`) are captured ONCE per request in `onHttpRequestHeaders` and stashed on the HttpContext (`ctxKeyAILabels`); never re-read inside the async callback or in `onHttpStreamDone`, both because of cost and because hostcalls between callback entry and SendHttpResponse are flagged in the redis-rejection hazard. The `bucket` label is the dimension fragment of the Redis key (everything past `<rule>|<combo>`) sanitised to `[a-zA-Z0-9_-]`; same bucket-id labels every metric for a given combo+match-result so operators can `topk()` on bucket and reverse-map back to the Redis key for `redis-cli ZRANGE` inspection. `metricCounters` is a process-global `sync.Map` cache because counter handles are owned by the host (idempotent across configs and across VM rebuilds). `request_total` is intentionally only labelled by `route, rule, result` so `sum(request_total)` is the real request count; per-combo breakdown lives on `rejected_total` and `value_total`. `rejected_total` is incremented *after* `rejected()` returns to keep the path between callback entry and `SendHttpResponseWithDetail` minimal (same hazard as the redis async case in CLAUDE.md).

**Fail-open posture**: Redis dispatch errors, Lua errors and unreadable `:path` all fall through to `ActionContinue`. The plugin never blocks traffic because of its own infrastructure failure.

**Rejection delivery hazards** — under Higress's AI-route filter chain, `SendHttpResponseWithDetail` will return `nil` (accepted by host) yet fail to flush to the downstream client. Symptom in access log: `response_code=429` + `bytes_sent=0` + `response_flags=DC` + `response_code_details=downstream_remote_disconnect` (or `via_wasm::...gpustack-rate-limit.rejected` for the local-backend variant). Three known triggers:

- **Empty response body**. `rejected_msg` falls back to `"Too many requests"` via `defaultRejectedMsg` for this reason — do not remove the fallback. Envoy's local-reply path treats headers+end_stream with a nil body pointer as a degenerate response and queues it without flushing.
- **Missing `ctx.DisableReroute()`** on request-phase entry. The async Redis dispatch races with other filters' route-cache invalidation otherwise.
- **Rejection body should be valid JSON when the request had `stream:true`**. `gpustack-token-usage` runs at priority 910 (>600), so on a streaming request it decodes the body first, sets `IsStreamingResponse=true`, and on the encode chain pipes every response chunk through `mergeLargeUsageChunks`. *Historically* that helper ran `json.Valid` on the whole block and treated non-JSON as an incomplete SSE fragment — buffering it into `IncompleteChunkData`, returning `nil`, and leaving the client with zero body bytes; that eating behavior is what originally forced the rejection to be JSON. The helper is now SSE-event-aware (`mergeSSEEventState`): a block with no `data:` field (which a raw rate-limit rejection body is) is forwarded verbatim, so even a single-chunk plain-text rejection now reaches the client. `rejected()` still wraps the message in an OpenAI-style `{"error":{"message":...,"type":"rate_limit_exceeded","code":N}}` envelope with `content-type: application/json` — keep it: JSON is the correct contract for AI clients, and a JSON body split across delivery chunks is forwarded fragment-by-fragment without `data:` re-prefixing (the old code would have prepended `data: ` to the completing fragment). If `RejectedMsg` is already a JSON object it is passed through verbatim.
- **`localBackend.BatchEval` must return `ActionPause` (==`HeaderStopIteration`=1) after a synchronous rejection, NOT `HeaderStopAllIterationAndWatermark` (4)**. The two pause-style actions are not interchangeable: 4 is the async-wait shape (apply backpressure / watermark, callback flushes the local reply later) used by `redisBackend`; 1 is the sync-stop shape (stop iterating *now*, flush whatever is queued) and is what Higress `key_rate_limit` (C++) returns after `sendLocalResponse`. After a sync `SendHttpResponseWithDetail` returning `ActionContinue` lets the filter chain keep iterating (body dropped — `bytes_sent=0`); returning `HeaderStopAllIterationAndWatermark` leaves the response stuck under watermark waiting for a resume that never comes (also `bytes_sent=0` with `response_code_details=via_wasm::...rejected`). The local backend therefore returns `ActionPause` from the limited branch.
- **`ResumeHttpRequest` ownership lives in the backend, not in `handleEvalResult`**. The redis backend's `cb` wrapper calls Resume after the success-branch metric emission completes (it is the one that returned `HeaderStopAllIterationAndWatermark` up front, so it is the one that owes the resume). The local backend never paused the request -- its success path returns `ActionContinue` synchronously -- and therefore must NOT call Resume. `handleEvalResult` is purely metric emission + rejection dispatch; it never calls Resume itself. An earlier iteration tried to centralise Resume in `handleEvalResult` and skip the success callback on the local backend to avoid an unnecessary Resume, but that silently dropped `request_total{result=passed}` and `value_total{kind=query}` on every passed request -- those metrics live inside the success branch of the callback. Fire the success callback on the local backend; just don't Resume.

Callback body is deliberately minimal (single `response.Array()` read, no `response.Error()` / `response.String()` / `GetProperty` / `LogInfof` between callback entry and `SendHttpResponseWithDetail`) to match the known-good shape of `cluster-key-rate-limit`. Extra hostcalls in between may confuse Envoy's async-local-reply path on some Higress builds — keep this pattern when refactoring.

### gpustack-ai-proxy

**Vendored fork** of the Higress `ai-proxy` plugin, copied verbatim from upstream and maintained here. Unlike the other plugins in this repo it is **not** an original implementation — treat upstream as the source of truth for everything except the local patches below. It is the largest plugin by far (~10 MiB wasm) and the only one with sub-packages (`config/`, `provider/`, `util/`, `test/`).

- **The upstream base commit lives in [FORK_BASE](extensions/gpustack-ai-proxy/FORK_BASE)** — machine-readable (`commit=`, `path=`, `upstream_version=`), and it is the single source of truth for "what are we forked from". The per-file attribution headers repeat the same commit; they must agree. Current base: higress [`aae6fbce36a2`](https://github.com/alibaba/higress/tree/aae6fbce36a2d1dd7afff007a265ecbebdd8a6f1/plugins/wasm-go/extensions/ai-proxy), upstream `VERSION` 2.0.1 → local `2.0.1-patched` (the `-patched` suffix marks the divergence).
- **Re-syncing is a wipe-and-replay, not a merge.** Delete the vendored tree, re-extract `git archive <commit> plugins/wasm-go/extensions/ai-proxy`, then replay the divergences in order: drop the four hgctl files, rewrite the module path, set `pluginName`, insert attribution headers immediately before each `package` line, suffix `VERSION`, re-insert the three README fork notices, re-apply the `mergeSystemMessages` patch, the deepseek `ApiNameResponses` capability and the Anthropic entries of the `FromJson` capability whitelist, rewrite `FORK_BASE`, `gofmt -w`. Verify by normalising the local tree back (strip headers, reverse the path rewrite, `gofmt` **both** sides — upstream ai-proxy is not gofmt-clean, so without this ~20 files show phantom import-order diffs) and diffing against the upstream tree: the only real divergence must be `claude_to_openai.go`, `claude_to_openai_test.go`, `deepseek.go`, `provider.go`, the three READMEs, `VERSION`, `go.mod`, `FORK_BASE`, the GPUStack-local test files that have no upstream counterpart (`deepseek_test.go`, `capabilities_anthropic_test.go`), plus header-only diffs in `main.go` and `test/provider_wasm_smoke.go` (their headers sit after a pre-existing leading comment, so a `head -1` normaliser misses them).

- **Functional divergence 1 — `mergeSystemMessages` in [claude_to_openai.go](extensions/gpustack-ai-proxy/provider/claude_to_openai.go)** (gpustack/gpustack#5934). Claude clients (notably Claude Code) send a system-role message *inside* the `messages` array **in addition to** Claude's top-level `system` field. Upstream's converter prepends the top-level `system` and leaves the embedded one where it was, producing two system messages with one of them not at index 0 — strict OpenAI-compatible backends (vLLM) then reject the request with `System message must be at the beginning.`. The patch collapses every system-role message into a single leading one: 0 → unchanged, 1 → preserved verbatim but moved to front, ≥2 → contents concatenated into one block array. `systemContentBlocks` normalises `string` vs `[]chatMessageContent` content so block-form system messages keep their `cache_control`.
- **Functional divergence 2 — `ApiNameResponses` in the deepseek provider's `DefaultCapabilities` ([deepseek.go](extensions/gpustack-ai-proxy/provider/deepseek.go))**. DeepSeek serves the OpenAI Responses API natively ([docs](https://api-docs.deepseek.com/zh-cn/guides/responses_api)), so this is a one-line path mapping, not a converter — everything else rides the generic path: no `TransformRequestBody` override (model mapping via `defaultTransformRequestBody`), no `GetApiName` override (main.go's `pathSuffixToApiName` already resolves `/v1/responses`), and `ApiNameResponses` is already in `isStreamingAPI` / `needToProcessRequestBody`. Notably **no request-body scrubbing** as doubao does for its unsupported `parallel_tool_calls` / `tool_choice`: DeepSeek documents that unsupported params are *silently ignored* (`静默忽略`) rather than rejected, so deleting them would only lose fidelity. The mapped path is `PathOpenAIResponses` (`/v1/responses`) — DeepSeek's docs show `base_url` without `/v1`, but `/v1` is its OpenAI-compat alias for the same endpoint (both answer 401, not 404, on a bad key) and it is the shape `ApiNameChatCompletion` / `ApiNameModels` already use in this same map. Pinned by [deepseek_test.go](extensions/gpustack-ai-proxy/provider/deepseek_test.go) (GPUStack-local, no upstream counterpart) because there is no conversion fallback: a dropped capability makes `OnRequestBody` return `errUnsupportedApiName` — a hard failure, not a degraded response.
- **Functional divergence 3 — the three Anthropic surfaces in the `capabilities` whitelist of `ProviderConfig.FromJson` ([provider.go](extensions/gpustack-ai-proxy/provider/provider.go))**. Upstream's `switch` drops any capability key it doesn't recognise, and `anthropic/v1/messages` (plus `count_tokens` / `complete`) was not in the list — so an operator could declare it and be silently ignored. That is not a cosmetic path-mapping loss: main.go's `onHttpRequestHeaders` rewrites an inbound `/v1/messages` to `/v1/chat/completions` and sets `needClaudeResponseConversion` **exactly when** `IsSupportedAPI(ApiNameAnthropicMessages)` is false, so a dropped key means the request gets protocol-converted anyway — indistinguishable from never having configured it, with no error and no log. Whitelisting the three keys lets any provider type that serves Claude natively say so from configuration instead of requiring a provider type that hardcodes them in `DefaultCapabilities`; capabilities are a top-up over `setDefaultCapabilities`, not a replacement, which is what makes this preferable to swapping `provider.type`. Pinned by [capabilities_anthropic_test.go](extensions/gpustack-ai-proxy/provider/capabilities_anthropic_test.go) (GPUStack-local, no upstream counterpart).
- **Every `.go` file carries a 4-line attribution header** naming its exact upstream URL at the fork commit. Keep it when editing — it is the only way to tell "we changed this" from "upstream looks like this" when re-syncing.
- **Four upstream files were deliberately deleted** because this repo builds with `extensions/Makefile` (plain `go build` → wasm) + `scripts/generate_metadata.py`, never `hgctl`: `option.yaml` (hgctl project config), `Makefile` (upstream's tinygo single-plugin build), `envoy.yaml` (hgctl local-test Envoy config), and `.gitignore` (hgctl's allowlist-style rules, which swallow `VERSION` and fight the root `.gitignore`). `README_dev.md` is upstream's hgctl dev doc, kept only for reference with a fork note prepended — it still describes the deleted `envoy.yaml`.
- `pluginName` is `"gpustack-ai-proxy"`, **not** upstream's `"ai-proxy"`. It flows into log prefixes and `response_code_details=via_wasm::...<pluginName>.<detail>`, so keeping upstream's value would make our fork indistinguishable from a real upstream `ai-proxy` filter in access logs.
- **Upstream `ai-proxy` is no longer fetched in `remote_plugins.yaml`** — same treatment as model-router/model-mapper. This fork is a drop-in replacement (config schema untouched), and shipping both would double ~10 MiB of wasm in the wheel for no benefit.
- `test/` holds upstream's hgctl e2e helper package. Nothing in the build or in `go test ./...` depends on it; it is retained purely to keep the diff against upstream small.

## Remote Plugins

Configured in `extensions/remote_plugins.yaml`. Require `oras` (`brew install oras`).

```yaml
default_registry: higress-registry.cn-hangzhou.cr.aliyuncs.com/plugins
remote_plugins:
  - source: ai-statistics:2.0.0    # uses default_registry
    name: ai-statistics
    version: 2.0.0
    digest: sha256:...         # optional, for pinning
  - source: oci://ghcr.io/higress-extensions/jwt-auth:1.1.0  # full OCI URL
    name: jwt-auth
    version: 1.1.0
```

`make build` fetches remote plugins automatically when `oras` is available; silently skips with a warning if not. Docker build always fetches them.

## Adding a New Plugin

1. Create `extensions/new-plugin/` with `main.go`, `go.mod`, `VERSION`
2. Add to `go.work` under the `use ()` directive
3. `make build`

## Excluding a Plugin from the Wheel (`.nopackage`)

Dropping a `.nopackage` file into a plugin directory keeps the source in the
repo but keeps its wasm out of the distribution. Currently used by
`gpustack-model-mapper` (6.0 MB raw / 1.7 MB deflated), which `gpustack-lb` in
`mode: context` replaces in place.

| Consumer | Behaviour |
| --- | --- |
| `extensions/Makefile` `build-all` | skips the directory |
| `extensions/Makefile` `list` | shows it tagged `[not packaged]` |
| `extensions/Makefile` `build PLUGIN_NAME=<name>` | **ignores the marker** — naming a plugin explicitly is the escape hatch |
| `extensions/Makefile` `test-all` | still runs its tests — the code is kept, so it must keep compiling |
| `scripts/verify_whl.py` | drops it from the expected set, and **fails** if it appears in the wheel anyway |

**Why a marker file rather than a list in the Makefile or a shared YAML**: the
Dockerfile's go-builder stage runs `make build-all PYTHON=true` with no Python
available (that is the point of the `PYTHON=true` trick), so it cannot parse a
YAML config; and `verify_whl.py` is invoked directly by the Dockerfile
(bypassing the Makefile), so it cannot receive a Makefile variable either. A
plain file next to the code is the only representation both sides read without
duplicating the fact. It survives go-builder's `rm -rf */*.go */go.mod */go.sum`.

The `verify_whl.py` failure on a present-but-excluded plugin is not defensive
padding: the wheel's `force-include` packages whatever sits in
`gpustack_higress_plugins/plugins/`, so a local `make build` over a stale
directory (from before the exclusion, or from a deleted plugin) silently ships
it again. `make clean` is the fix.

## CI

- **ci.yaml**: runs on push to main/develop and PRs — Go 1.24 + Python 3.11, `make test` → `make lint` → `make check-dirty` → Docker `whl-output` build (same Dockerfile as push/release: compiles Go, fetches remote plugins via oras, runs full `verify_whl.py`)
- **push.yaml**: runs on push to main — builds `whl-output` via Dockerfile (includes remote plugins via oras), uploads `.whl` as GitHub artifact `gpustack-higress-plugins-dist` (7 days), then builds and pushes `linux/amd64,linux/arm64` Docker image with tags `:latest`/`:main`; override registry with `vars.DOCKER_REPO`
- **release.yaml**: triggered by version tags (`v*.*.*` or `*.*.*`) — syncs `pyproject.toml` version from tag, builds whl + image via Dockerfile; uploads `.whl` as release asset; RC tags (`*rc*`) → TestPyPI + Docker without `:latest`; stable tags → PyPI + Docker with `:latest`

## Pre-commit

`.pre-commit-config.yaml` runs `ruff` (lint + fix) and `ruff-format` on `gpustack_higress_plugins/` and `scripts/` before each commit. Installed automatically by `make dev`.
